
export interface Product {
  id: string;
  name: string;
  description: string;
  sensoryNote: string;
  price: string;
  image: string;
}

export interface Testimonial {
  id: string;
  quote: string;
  author: string;
}
